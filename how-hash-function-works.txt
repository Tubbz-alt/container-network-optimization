skb_get_hash(struct sk_buff *skb)
--> __skb_get_hash(struct sk_buff *skb)
--> ___skb_get_hash(const struct sk_buff *skb, struct flow_keys *keys, u32 keyval) //return hash
{
	--> skb_flow_dissect_flow_keys(const struct sk_buff *skb,
					      struct flow_keys *flow,
					      unsigned int flags)
					      //generate keys based on skb
        --> __skb_flow_dissect(const struct sk_buff *skb,
				struct flow_dissector *flow_dissector,
				void *target_container,
				void *data, __be16 proto, int nhoff, int hlen,
				unsigned int flags)
				//generate the flow keys based on the skb data


	--> __flow_hash_from_keys(struct flow_keys *keys, u32 keyval)
	    //get hash based on the info inside keys

	    --> __flow_hash_words(const u32 *words, u32 length, u32 keyval)
	        //input: keys; output: hash value
	        //key length is 7 in our local environment

	        --> jhash2(const u32 *k, u32 length, u32 initval)
	            //hash an array of u32's (it is the keys)
}
